spring:
  datasource:
    url: jdbc:postgresql://${PGHOST}:${RAILWAY_TCP_PROXY_PORT}/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate